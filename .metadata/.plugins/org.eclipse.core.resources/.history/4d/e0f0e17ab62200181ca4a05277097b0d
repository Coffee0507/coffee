/**
 *
 */
export count doIt = () => {
	return (dispatch) => {
		dispatch(showLoading());
		return dispatch(doHttpAction()).then((res) => {
			dispatch(hideLoding());
		})dispatch(refreshDisplay(res.date));
	})
	.catch(() => {
		dispatch(hideLoading());
	});
};
};