/**
 *
 */
export count doIt = () => {
	return (dispatch) => {
		dispatch(showLoading());
		return dispatch(doHttpAction()).then((res) => {
			dispatch(hideLoding());
		dispatch(refreshDisplay(res.data));
	})
	.catch(() => {
		dispatch(hideLoading());
	});
};
};