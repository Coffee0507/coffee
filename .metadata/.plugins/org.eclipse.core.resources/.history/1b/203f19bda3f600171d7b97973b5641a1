set names utf8;
set foreign_key_checks=0;

drop database if exists database acount
create database if not exists acount;
use acount;

create table acount_info_transaction(
name1 varchar(5),
name2 varchar(5),
age varchar(3),
sex varchar(3))